import { connect } from "react-redux";
import TaskThree from './TaskThree';

const mapStateToProps = () => {
  return {

  }
};

const mapDispatchToProps = () => {
  return {

  }
};

const TaskThreeContainer = connect(mapStateToProps, mapDispatchToProps)(TaskThree);

export default TaskThreeContainer;